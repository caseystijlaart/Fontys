.vscode/settings.json

# C++ build artifacts
src/firmware/build/
compile_commands.json

# Python caches
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage

# Coverage reports (badges/*.svg are committed by CI, the raw reports are not)
coverage.json
src/firmware/coverage.json
